# Improve Manage Composer, Nginx, PostgreSQL, and MySQL

I want to improve the Composer, Nginx, PostgreSQL, and MySQL in "Manage Services" tab.

## Proposed Enhancement

- Background color, border color, button color isn't matching with the Composer theme. Please Update the colors to match the Composer theme.
- Background color, border color, button color isn't matching with the Nginx theme. Please Update the colors to match the Nginx theme.
- Background color, border color, button color isn't matching with the PostgreSQL theme. Please Update the colors to match the PostgreSQL theme.
- Background color, border color, button color isn't matching with the MySQL theme. Please Update the colors to match the MySQL theme. 
- Improve UI in the dark mode
- Don't asking to run "npm run lint"
- Don't create any unnecessary markdown files.
- Don't asking to write tests or run build
- Optimize the code for better performance and readability.